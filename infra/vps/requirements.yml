---
# ChrisShop Ansible Collection Dependencies
# Install with: ansible-galaxy collection install -r infra/vps/requirements.yml

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
